---
apiVersion: core.openfeature.dev/v1alpha3
kind: FlagSourceConfiguration
metadata:
    name: source-configuration
spec:
    metricsPort: 8080
    evaluator: json
    defaultSyncProvider: filepath
    # renovate: datasource=github-releases depName=open-feature/flagd extractVersionTemplate=^flagd/(?<version>.*)$
    tag: v0.5.3
    sources:
        - source: end-to-end-test
          provider: filepath
    probesEnabled: true
